# docker-compose.yml
version: '3.8'

services:
  nilman-app:
    build: .
    container_name: nilman-container
    ports:
      - "3003:3003"
    restart: unless-stopped
    environment:
      - NODE_ENV=production